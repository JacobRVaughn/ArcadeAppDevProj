document.addEventListener("DOMContentLoaded", () => {
    const openButton = document.getElementById("viewAllButton");
    // TODO: create close button and full leaderboard elements

    function openFullLeaderboard() {

    }

    function closeFullLeaderboard() {

    }

    openButton.addEventListener("click", (e) => {
        e.preventDefault();
        openFullLeaderboard();
    });

    // TODO: closeButton.addEventListner(...);
    // maybe esc button to close menu too?
    

});